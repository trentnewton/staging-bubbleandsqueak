title: Hero
'@extends': default

form:
  fields:
    tabs:
      fields:
        content:
          type: tab
          # title: Hero
          fields:
            header.subtext:
              type: markdown
              label: Sub Text
            header.hero_image:
              type: filepicker
              label: Hero Image
              preview_images: true
              description: 'If not specified, this defaults to the first image found in the page''s folder'


